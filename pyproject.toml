[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pet-harmonization"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["pet_harmonization*"]